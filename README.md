# New-Project

Repositório com dois projetos independentes.

| Pasta                                              | Projeto                                                                                |
| -------------------------------------------------- | -------------------------------------------------------------------------------------- |
| [`comissoes-vendedores/`](./comissoes-vendedores/) | Plataforma de controle de comissões de vendedores (React + Express). Veja o README dele. |
| Raiz (`index.html`, `scripts.js`, `styles.css`)    | Conversor de moedas em HTML/CSS/JS puro — abra o `index.html` no navegador.               |

A CI em `.github/workflows/ci.yml` roda lint, testes e build de `comissoes-vendedores/`.
